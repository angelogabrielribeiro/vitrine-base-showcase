name: Fix Maison reveal duration scope

on:
  pull_request:
    branches:
      - main
    paths:
      - "scripts/fix-maison-reveal-duration-scope.py"
      - ".github/workflows/fix-maison-reveal-duration-scope.yml"

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
        with:
          ref: fix/maison-scroll-layout-stage
          fetch-depth: 0

      - uses: actions/setup-node@v4
        with:
          node-version: 22

      - run: npm install --no-audit --no-fund
      - run: python scripts/fix-maison-reveal-duration-scope.py
      - run: |
          npx prettier --write scripts/validate-maison-scroll-layout.mjs
          node --check scripts/validate-maison-scroll-layout.mjs

      - name: Commit test correction
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add scripts/validate-maison-scroll-layout.mjs
          git commit -m "Corrige escopo da regressão Maison [maison test scope]"
          git push origin HEAD:fix/maison-scroll-layout-stage
