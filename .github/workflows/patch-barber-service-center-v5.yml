name: Patch Barber service center v5

on:
  pull_request:
    branches:
      - main
    paths:
      - ".github/barber-service-center-v5.trigger"

permissions:
  contents: write

jobs:
  patch:
    if: github.event.pull_request.head.ref == 'fix/barber-mobile-cards-stage'
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - uses: actions/checkout@v4
        with:
          ref: fix/barber-mobile-cards-stage
          fetch-depth: 0

      - name: Apply patch
        run: python .github/scripts/patch-barber-service-center-v5.py

      - name: Format patch
        run: |
          npm install --no-audit --no-fund
          npx prettier --write \
            src/components/storefront/barber-storefront.tsx \
            scripts/validate-barber-mobile-cards.mjs \
            .github/workflows/validate-barber-mobile-cards.yml
          git checkout -- package-lock.json 2>/dev/null || true

      - name: Commit patch
        run: |
          git rm \
            .github/scripts/patch-barber-service-center-v5.py \
            .github/barber-service-center-v5.trigger
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add \
            src/components/storefront/barber-storefront.tsx \
            scripts/validate-barber-mobile-cards.mjs \
            .github/workflows/validate-barber-mobile-cards.yml
          git commit -m "Make Barber service activation follow viewport center"
          git push origin HEAD:fix/barber-mobile-cards-stage

      - name: Remove workflow from main
        if: always()
        run: |
          git fetch origin main
          git worktree add -B barber-main-v5-cleanup /tmp/vitrine-main-v5 origin/main
          cd /tmp/vitrine-main-v5
          git rm -f .github/workflows/patch-barber-service-center-v5.yml
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git commit -m "Remove isolated Barber service workflow"
          git push origin HEAD:main

      - name: Remove branch helpers after failure
        if: failure()
        run: |
          cd "$GITHUB_WORKSPACE"
          git fetch origin fix/barber-mobile-cards-stage
          git reset --hard origin/fix/barber-mobile-cards-stage
          git rm -f \
            .github/scripts/patch-barber-service-center-v5.py \
            .github/barber-service-center-v5.trigger
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git commit -m "Remove failed Barber service v5 helpers"
          git push origin HEAD:fix/barber-mobile-cards-stage
